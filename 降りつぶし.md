# 降りつぶし 設計メモ

このファイルは、この会話時点で確定している内容を整理したものです。実装方針ではなく、まずはテーブル設計の前提をまとめています。

採用DBは Supabase とし、RDBMS は PostgreSQL を前提とする。

## mst_company_type

| カラム名 | 型の提案 | 特徴 |
|---|---|---|
| `id` | `INT` | 会社種別の識別子。主キー |
| `name` | `VARCHAR(255)` | 会社種別名 |

特徴:
- 主キーは `id`
- `mst_company.company_type` から参照するコードマスタ

## mst_company

| カラム名 | 型の提案 | 特徴 |
|---|---|---|
| `id` | `BIGINT` | 会社実体の識別子。`revision` と組で主キー |
| `revision` | `INT` | 履歴版番号 |
| `company_type` | `INT` | `mst_company_type.id` を参照する会社種別 |
| `name` | `VARCHAR(255)` | 会社名 |
| `note` | `TEXT` | 備考。NULL許容 |
| `is_deleted` | `BOOLEAN` | 現行版は `false`、旧版は `true` |

特徴:
- 主キーは `(id, revision)`
- 履歴保持型マスタ
- 旧版は `is_deleted = true` で表す

## mst_line

| カラム名 | 型の提案 | 特徴 |
|---|---|---|
| `id` | `BIGINT` | 路線実体の識別子。`revision` と組で主キー |
| `revision` | `INT` | 履歴版番号 |
| `name` | `VARCHAR(255)` | 路線名 |
| `company_id` | `BIGINT` | `mst_company.id` を参照 |
| `display_start_station_id` | `BIGINT` | 駅順表示の起点駅。非環状線では実質始発、環状線では表示用の起点 |
| `note` | `TEXT` | 備考。NULL許容 |
| `is_deleted` | `BOOLEAN` | 現行版は `false`、旧版は `true` |

特徴:
- 主キーは `(id, revision)`
- `company_revision` は持たない
- 終点駅は持たない
- 駅順の復元は `display_start_station_id` と `mst_section` を使う

## mst_station

| カラム名 | 型の提案 | 特徴 |
|---|---|---|
| `id` | `BIGINT` | 駅実体の識別子。`revision` と組で主キー |
| `revision` | `INT` | 履歴版番号 |
| `name` | `VARCHAR(255)` | 駅名 |
| `company_id` | `BIGINT` | `mst_company.id` を参照。会社が違えば別駅として扱う |
| `is_shinkansen` | `BOOLEAN` | 新幹線駅かどうか |
| `first_achieved_on` | `CHAR(8)` | 初回利用日を表す8桁コード。NULL許容 |
| `latitude` | `DECIMAL(10,7)` | 緯度。NULL許容 |
| `longitude` | `DECIMAL(10,7)` | 経度。NULL許容 |
| `note` | `TEXT` | 備考。NULL許容 |
| `is_deleted` | `BOOLEAN` | 現行版は `false`、旧版は `true` |

`first_achieved_on` のルール:
- `NULL`: 未達成
- `YYYYMMDD`: 年月日が分かる
- `YYYYMM99`: 日不明
- `YYYY9999`: 月日不明
- `99999999`: 利用済みだが年不明

特徴:
- 主キーは `(id, revision)`
- `line_id` は持たない
- 駅位置情報として `latitude` / `longitude` を追加
- 緯度経度は別カラム、どちらもNULL許容

## mst_section

| カラム名 | 型の提案 | 特徴 |
|---|---|---|
| `id` | `BIGINT` | 区間実体の識別子。`revision` と組で主キー |
| `revision` | `INT` | 履歴版番号 |
| `line_id` | `BIGINT` | `mst_line.id` を参照 |
| `from_station_id` | `BIGINT` | 路線定義上の起点側駅 |
| `to_station_id` | `BIGINT` | 路線定義上の隣接駅 |
| `distance` | `DECIMAL(6,1)` | 区間距離。単位km。未公表はNULL |
| `first_achieved_on` | `CHAR(8)` | 区間の初回達成日。双方向共通で記録 |
| `note` | `TEXT` | 備考。NULL許容 |
| `is_deleted` | `BOOLEAN` | 現行版は `false`、旧版は `true` |

特徴:
- 主キーは `(id, revision)`
- `from_station_id -> to_station_id` は駅順管理のための向き
- 利用実績は逆方向乗車でも同一区間として達成扱い
- 環状線も通常路線も同じ構造で表現する

## 確定している制約・ルール

- `revision` は全テーブル共通で `0` から始める
- `mst_section` では、同じ `line_id` で同じ `from_station_id` を複数持てない
- `mst_section` では、`from_station_id = to_station_id` は禁止
- 環状線判定は、起点駅から駅をたどり、再び起点駅に到達した時点で探索を打ち切り、環状線とみなす
- `mst_station.latitude` と `mst_station.longitude` はNULL許容
- `mst_section.distance` はNULLで未公表を表す

## フェーズ1のWeb UI方針

フェーズ1では、スマートフォンでアクセスするWebサイトとして、以下を優先する。

- 駅の乗降車記録
- 区間の乗車記録
- 記録の閲覧

一方で、以下はフェーズを分けて後続対応とする。

- 路線名変更
- 駅の新設、廃止
- 区間の変化
- マスターデータのメンテナンス機能
- `mst_company.note` の登録、編集
- `mst_line.note` の登録、編集

## トップ画面

- 初期表示は現在地の地図
- 現在地が取得できない場合は東京駅付近をフォールバック表示とする
- 初期ズームは固定値で表示する
- 地図中心の周辺駅を最大10駅まで表示する
- 地図は `mst_station.latitude` と `mst_station.longitude` を使って駅ピンを表示する
- 駅ピンの色は以下とする
  - 青: `mst_station.first_achieved_on` が設定済み
  - 赤: `mst_station.first_achieved_on` が `NULL`
- 画面上部のタイトルは `降りつぶし` のみ表示する
- タイトルの上下の補助ラベルは表示しない
- タイトル下の説明文は表示しない

画面下部には以下のタブを置く。

- `地図を見る`
- `記録する`
- `記録を見る`

各タブの扱いは以下とする。

- `地図を見る`
  - 地図トップへ遷移する
- `記録する`
  - 記録画面に遷移する
- `記録を見る`
  - 保留中であることを示すプレースホルダ画面を表示する

地図下の駅情報は以下とする。

- 初期状態では駅一覧を表示しない
- 初期状態では `ピンをタップすると駅情報を表示します。` の注釈のみ表示する
- ピンをタップしたときだけ、対象駅の詳細カードを地図下に表示する
- 駅詳細カードの表示項目は以下とする
  - 駅名
  - `会社名:`
  - `初乗降車日:`
  - `備考:`
- 駅詳細カードではラベルと値の頭出しを揃える
- 駅詳細カードでは `会社名` の横に距離を表示しない
- 備考がない場合は `なし` と表示する
- 駅詳細カード自体には遷移リンクを置かない

## 地図上の駅ピンタップ時の表示

駅ピンをタップした際は、以下のみ表示する。

- 駅名
- 会社名
- 備考
- 乗降車記録画面へ遷移するボタン

この画面では、会社備考の登録、編集は行わない。

乗ったか降りたかの区別は持たず、まとめて乗降車として扱う。

同一座標に複数駅がある場合は以下とする。

- 同一緯度経度の駅は1つのピンにまとめる
- 同じピンをタップするたびに、表示駅を順送りで切り替える
- 駅の表示順は取得順でよい
- 複数駅がある場合、ポップアップに `同位置に n 駅あります。タップで切替。` を赤字で表示する

## 記録する導線

`記録する` タブからの遷移は以下とする。

- 会社種別一覧
- 会社一覧
- 路線一覧
- 路線詳細兼記録画面

会社配下の路線一覧は `mst_line.id` の昇順で表示する。

`記録する` 画面自体の表示は以下とする。

- 画面上部のタイトルは `降りつぶし` のみ表示する
- `記録するTop` のパンくずを表示する
- 路線一覧では `起点駅ID ...` の補助文は表示しない
- 記録トップの会社種別一覧は `mst_company_type.id` が `1` から `4` までを表示する
- 会社種別 `3` を選んだ場合は、会社種別 `3` と `5` の会社をまとめて会社一覧に表示する
- 会社種別名に ` (1路線のみ)` が含まれる場合でも、画面表示とパンくずではこの注記を外して表示する

会社一覧、路線一覧、駅記録、区間記録では、戻りやすさを優先してパンくずを表示する。

会社一覧、路線一覧では、画面補助文として次画面への説明を短く表示してよい。

## 路線詳細兼記録画面

画面上部には以下を固定表示する。

- 会社名
- 路線名

画面上には `駅区間一覧` などの開発用名称は表示しない。

長大路線でも、スクロールするのは駅と区間の一覧表示部のみとする。
- 路線名エリアは縦スクロール時に上部へ固定する
- 路線名エリアはフォントサイズを変えずに上下余白を詰める
- 路線詳細画面では、タイトルカードは固定表示とし、その下の駅区間一覧エリア内だけを縦スクロールさせる
- 駅区間一覧エリアのスクロールバーは表示しない

一覧の並び方は以下とする。

- `mst_line.display_start_station_id` を起点に並べる
- 非環状線は、起点駅から終着駅まで順に並べる
- 環状線は、起点駅からたどり、再び起点駅に到達した時点で探索を終了する

表示内容は以下とする。

- 駅行
  - 駅名
  - `mst_station.first_achieved_on`
  - レイアウトに余裕があれば備考
  - 達成済みは青バッジ、未達成は赤バッジで表示する
  - 未達成文言は `未乗降車` とする
- 区間行
  - 下矢印を表示して駅間であることを分かりやすくする
  - `mst_section.distance`
  - `mst_section.first_achieved_on`
  - 達成済みは青バッジ、未達成は赤バッジで表示する
  - 未達成文言は `未乗降車` とする
  - バッジ幅は内容幅とし、駅行と同じ見え方にする

駅名と駅名の間に、対応する区間情報を表示する。

この画面では、路線備考の登録、編集は行わない。

## 路線詳細兼記録画面からの遷移

- 駅行タップ
  - 駅記録画面へ遷移する
- 区間行タップ
  - 区間記録画面へ遷移する

`地図を見る` から駅記録画面へ遷移した場合は、`戻る` で `地図を見る` に戻す。

## first_achieved_on の共通入力仕様

駅記録画面、区間記録画面の `first_achieved_on` は共通UIで入力する。

- `年` `月` `日` の3つのドラムロールを用意する
- 各ドラムロールには通常値に加えて `不明` を用意する
- 入力確定時に8桁コードへ変換して保存する
- `NULL` は別扱いとし、`未乗降車` または `クリア` 操作で設定する

## 2026-07-31 実装メモ

この日までに実装・調整した内容は以下のとおり。

### UI/UX の調整
- 記録フローで、会社一覧・路線一覧・駅記録・区間記録にパンくずリストを追加した。
- 記録トップ画面にも「記録するTop」のパンくずを表示するようにした。
- 駅記録・区間記録遷移時に、選択中の路線名がパンくずに残るように、遷移クエリに路線情報を付与した。
- `first_achieved_on` の入力UIとして、年・月・日を並べたドラムロール風の入力を整えた。
- 年・月・日それぞれの間に見出し文字を挿し、日入力の右側にも「日」を表示するようにした。
- ドラムロールを囲む四角枠は外し、よりシンプルな見た目にした。

### バグ修正
- 区間記録画面で発生していた Leaflet 関連のランタイムエラーを解消した。
- Leaflet の CSS / クライアント実装を地図専用コンポーネント側に寄せ、他ページで不要に読み込まれないようにした。
- 開発サーバー再起動後に、アクセス先が正しく反映されることを確認した。

### 確認方法
- 開発サーバーはローカルで起動し、以下の URL で確認できる。
  - `http://localhost:3000/`
  - `http://localhost:3000/record`
  - `http://localhost:3000/record/line/2253`
  - `http://localhost:3000/stations/44374`
  - `http://localhost:3000/sections/34674`

入力制約は以下とする。

- 年が `不明` の場合、月と日は `不明` のみ選択可
- 月が `不明` の場合、日は `不明` のみ選択可
- 月が実値の場合、日はその月に存在する日だけ選択可

変換ルールは以下とする。

- `年=2026, 月=01, 日=01` → `20260101`
- `年=2026, 月=01, 日=不明` → `20260199`
- `年=2026, 月=不明, 日=不明` → `20269999`
- `年=不明, 月=不明, 日=不明` → `99999999`

逆変換も可能とする。

- `20260101` → `2026 / 01 / 01`
- `20260199` → `2026 / 01 / 不明`
- `20269999` → `2026 / 不明 / 不明`
- `99999999` → `不明 / 不明 / 不明`

画面表示時の文言は以下とする。

- `NULL` → `未乗降車`
- `99999999` → `不明`
- `YYYY9999` → `YYYY年頃`
- `YYYYMM99` → `YYYY年M月頃`
- `YYYYMMDD` → `YYYY年M月D日`

## 駅記録画面

駅記録画面は、対象駅の情報を確認しながら `mst_station.first_achieved_on` を記録するための画面とする。

表示構成は以下とする。

- 画面上部に駅名を大きく表示する
- その下に、少しフォントを落として以下を表示する
  - 会社名
- その下に、備考を入力するためのテキスト入力部品を配置する
- その下に、記録用のドラムロールを3つ並べる
  - 年
  - 月
  - 日

ドラムロールの仕様は、前述の `first_achieved_on` の共通入力仕様に従う。

この画面で編集対象とするのは以下である。

- `mst_station.first_achieved_on`
- `mst_station.note`

## 区間記録画面

区間記録画面は、対象区間の情報を確認しながら `mst_section.first_achieved_on` を記録するための画面とする。

表示構成は以下とする。

- 画面上部に `From駅→To駅` を表示する
- その下に、備考を入力するためのテキスト入力部品を配置する
- その下に、記録用のドラムロールを3つ並べる
  - 年
  - 月
  - 日

ドラムロールの仕様は、前述の `first_achieved_on` の共通入力仕様に従う。

この画面で編集対象とするのは以下である。

- `mst_section.first_achieved_on`
- `mst_section.note`

## フェーズ2へ回す内容

フェーズ1のUIでは扱わず、フェーズ2へ回す内容は以下とする。

- `mst_company.note` の登録、編集
- `mst_line.note` の登録、編集
- 路線名変更への対応
- 駅の新設、廃止への対応
- 区間の変化への対応
- マスターデータのメンテナンス機能全般

## フェーズ1 実装確定事項

ここから下は、実装を進める中で確定したUI文言と挙動を整理したものである。上記の設計方針より優先して扱う。

### タブ文言

- `現在地周辺` は画面表示上 `地図を見る` に変更する
- `記録する` はそのまま使う
- `記録を見る` はそのまま使う

### 地図を見る 画面

- 画面上部のタイトルは `降りつぶし` のみ表示する
- タイトルの上下に補助ラベルは置かない
- タイトル下の説明文も表示しない
- 状態表示文言は `地図の中心の周辺10駅を表示` とする

地図下の駅情報表示は以下とする。

- 初期状態では駅一覧を出さない
- 初期状態では `ピンをタップすると駅情報を表示します。` の注釈のみ表示する
- ピンをタップしたときだけ、その駅の詳細カードを地図下に表示する

駅詳細カードの表示項目は以下とする。

- 駅名
- `会社名:`
- `初乗降車日:`
- `備考:`

表示ルールは以下とする。

- ラベルと値は頭出しを揃える
- 駅名は明細より大きく表示する
- `会社名` の横に距離は表示しない
- 備考がない場合は `なし` と表示する

地図ピンのポップアップは以下とする。

- 駅名
- 会社名
- 備考
- 乗降車記録画面へ遷移するリンク

ポップアップでは以下を表示しない。

- 初乗降車日

同一座標に複数駅がある場合の扱いは以下とする。

- 同一緯度経度の駅は1つのピンにまとめる
- 同じピンをタップするたびに、表示駅を順送りで切り替える
- 例: A駅 → B駅 → C駅 → A駅
- 駅の順序は取得順でよい
- 複数駅がある場合、ポップアップに `同位置に n 駅あります。タップで切替。` を赤字で表示する

### 初乗降車日の表示文言

`first_achieved_on` の表示文言は以下とする。

- `NULL` → `未乗降車`
- `99999999` → `不明`
- `YYYY9999` → `YYYY年頃`
- `YYYYMM99` → `YYYY年M月頃`
- `YYYYMMDD` → `YYYY年M月D日`

### 地図を見る からの駅記録画面遷移

- `地図を見る` から駅記録画面へ遷移した場合、`戻る` は `地図を見る` に戻す
- 会社一覧や路線一覧から駅記録画面へ遷移した場合は、従来どおりその導線側へ戻す

### 記録する 導線の画面文言

- `記録する` 画面のタイトルは `降りつぶし` のみ表示する
- タイトルの上下に補助ラベルは置かない
- タイトル下の説明文も表示しない
- 路線一覧では、`起点駅ID ...` の補助文は表示しない

### 路線タップ先画面

この画面は開発上 `駅区間一覧` と呼ぶが、画面文言としては表示しない。

画面の扱いは以下とする。

- 画面上部には路線名と会社名のみ表示する
- 路線名エリアは縦スクロール時に上部へ固定する
- フォントサイズは維持しつつ、路線名エリアの上下余白は詰める

駅行の表示は以下とする。

- 達成済みは青バッジ
- 未達成は赤バッジ
- 未達成文言は `未乗降車`

区間行の表示は以下とする。

- 駅間であることが分かるように下矢印を表示する
- 距離と区間達成日を見やすく配置する
- 達成済みは青バッジ
- 未達成は赤バッジ
- 未達成文言は `未乗降車`
- バッジ幅は内容幅とし、駅行と同じ見え方にする

### 記録を見る 導線

`記録を見る` タブのトップ画面には、以下の3つのボタンを表示する。

- `日付別`
- `都道府県別`
- `乗降車記録出力`

初期実装では `日付別` のみ有効とし、`都道府県別` と `乗降車記録出力` は disabled 表示にする。

#### 日付別

- 対象は `駅` のみとする
- `日付別` をタップすると、年単位の集計結果を表示する
- 各年は次画面へのボタンとする
- 年をタップすると、その年の月単位の集計結果を表示する
- 月をタップすると、その月の日単位の集計結果を表示する
- 日をタップすると、その日に乗降車した駅一覧を表示する
- 駅をタップすると、駅記録画面へ遷移する

#### 都道府県別

- 対象は `駅` のみとする
- `都道府県別` をタップすると、都道府県単位の集計結果を表示する
- 各都道府県は次画面へのボタンとする
- 都道府県をタップすると、その都道府県の駅一覧を表示する
- 駅をタップすると、駅記録画面へ遷移する
- 都道府県は `mst_station` に今後追加予定の列を利用する前提とする

#### 乗降車記録出力

- `乗降車記録出力` をタップすると、乗降車記録をファイル出力する
- 出力形式の優先順位は以下とする
- 第1候補は Excel (`.xlsx`)
- 第2候補は Google スプレッドシート
- 第3候補は CSV
- この優先順位は実装方式を検討する際の優先順位を表す
- たとえば Excel 方式を採用した場合、出力失敗時に自動で Google スプレッドシートや CSV へフォールバックはしない
- ファイル名は `乗降車記録_yyyy-mm-dd.xlsx` とする
- `yyyy-mm-dd` にはシステム日付を用いる
- 出力対象は `is_deleted = false` の現行データのみとする
- 出力ボタンは、出力中であることが分かる表示を行い、処理中は二度押しできないようにする

出力単位は以下とする。

- 会社別にシートを分ける
- シート名は会社名とする
- ただし、会社種別が `5` の会社はまとめて1シートに出力する
- その場合のシート名は `その他` とする
- シートの出力順は会社種別の昇順、会社IDの昇順とする

各シート内の出力ルールは以下とする。

- 路線の出力順は路線IDの昇順とする
- 路線ごとに1列空けて横並びで出力する
- 路線名の出力位置は `A1`, `F1`, `K1`, ... とする
- `A1` には路線名を出力する
- 会社種別が `5` の場合は、路線名の前に会社名と半角スペースを付ける
- `A2` に `駅名`、`B2` に `駅間` と改行して `距離`、`C2` に `乗降車日`、`D2` に `備考` を出力する
- `A3` には路線の始発駅を出力し、以降は駅区間一覧画面の並びに合わせて出力する
- 駅行では `A` 列に駅名、`C` 列に駅の乗降車日、`D` 列に駅の備考を出力し、`B` 列は空欄とする
- 区間行では `B` 列に区間距離、`C` 列に区間の乗車日、`D` 列に区間の備考を出力し、`A` 列は空欄とする
- 区間距離は数値として出力し、小数第1位まで表示する
- 乗降車日が完全な日付で分かる場合は Excel の日付値として出力し、表示形式は `yyyy/mm/dd` とする
- `不明` や `〜頃` のような曖昧な日付は文字列として出力する

セル書式は以下とする。

- 1行目の路線名セルは罫線なし、塗りつぶしなし、左寄せとする
- 2行目のヘッダー部は実線格子にする
- 2行目のヘッダー部の背景色は `#D9E1F2` とする
- 2行目のヘッダー部はセル内左寄せとする
- データ部は外枠を実線にする
- データ部の内枠は、横罫線を破線、縦罫線を実線にする
- データ部で `乗降車日` 列に値が存在する場合、その行の背景色を `#BFBFBF` とする
- 罫線の色は黒とする
- フォントは `Noto Sans JP`、10pt とする
- 路線間に空ける列の幅は `1` とする
- それ以外の列幅は、内容が隣のセルにはみ出さないよう自動調整する
- 2行目ヘッダーの高さは、`駅間` と `距離` の2行表示が潰れない高さにする
- データ部の行の高さは固定しない

### 記録を見る 実装メモ

現時点の実装状況は以下のとおり。

- `記録を見る` トップ画面には `記録を見るTop` のパンくずと、タイトル `降りつぶし` を表示する
- トップ画面のボタンは `日付別`、`都道府県別`、`乗降車記録出力` を有効とする
- `日付別` では、年別集計、月別集計、日別集計、駅一覧まで画面遷移できる
- `都道府県別` では、都道府県一覧と駅一覧まで画面遷移できる
- 年別、月別、日別、都道府県別の各画面にも下部タブを表示する

`不明` の扱いは以下とする。

- 年別集計の `不明` は、月別を挟まずそのまま駅一覧を表示する
- 月別集計の `不明` は、日別を挟まずそのまま駅一覧を表示する
- 日別集計では `日不明` を `不明` として一覧に含める

駅一覧の表示は以下とする。

- 駅名を主表示とする
- 補助情報として会社名を表示する
- 会社名の下に備考を表示する。文言は `note: ` を先頭に付ける
- `is_shinkansen = true` の駅は、駅名の右に `新幹線` ラベルを表示する
- 駅をタップすると駅記録画面へ遷移する

駅一覧の見た目は以下とする。

- 日付別の駅一覧は、すべて達成済み駅として緑系カードで表示する
- 都道府県別の駅一覧は、達成済みを緑系、未達成をオレンジ系で表示する
- 都道府県別の駅一覧では、右上に `済` / `未` の状態バッジを表示する

都道府県別の集計表示は以下とする。

- 対象は `is_deleted = false` かつ `prefecture` が設定されている駅のみとする
- 都道府県一覧では、各都道府県に `済の駅数 / 総数` を表示する
- 都道府県別トップのタイトル下にも、全都道府県合算の `済の駅数 / 総数` を表示する
- 各都道府県の詳細ページでも、都道府県名の下に `済の駅数 / 総数` を表示する

日付別の集計表示は以下とする。

- 年別集計トップのタイトル下には `済の駅数 / 総数` を表示しない

駅一覧の並び順は以下とする。

- 日付別の駅一覧は駅IDの昇順とする
- 都道府県別の駅一覧は駅IDの昇順とする

履歴表示時の駅名採用ルールは以下とする。

- 同一 `mst_station.id` の履歴をすべて参照する
- `first_achieved_on` を持つ最小 `revision` を、その駅の履歴表示用スナップショットとして採用する
- 日付別および都道府県別の駅名、備考、新幹線フラグは、この履歴表示用スナップショットを用いる
- 会社名は履歴考慮せず、現行の `company_id` に紐づく会社名を表示する

駅記録画面、区間記録画面の下部タブは以下とする。

- 下部タブを表示する
- `記録を見る` から遷移した場合も、選択状態は `記録する` とする

履歴集計の実装上の注意は以下とする。

- Supabase の一覧取得は1回の取得件数に上限があるため、履歴集計では全件取得してから集計する
- 年別、月別、日別、都道府県別、`不明` 駅一覧のいずれも同じ全件取得結果を用いて集計する
